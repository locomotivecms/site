%section.company-crew-person
  .rounded-picture
    %img{ :src => "{{ person.picture.url }}", :alt => "" }
  %header
    %h1 {{ person.name }}
    %span.position {{ person.title }}
  {{ person.bio }}
    