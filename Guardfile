guard 'livereload' do
  watch(%r{app/views/.+\.(erb|haml|slim|liquid)})
  watch(%r{public(/stylesheets/.+\.css)})
  watch(%r{public(/stylesheets/pages.+\.sass)})
  watch(%r{public(/javascripts/.+\.js)})
end